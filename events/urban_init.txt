name = urban_init

urban_init.1 = { # 
	type = country_event
	placement = root

    trigger = {
        # triggered at game start
        game_date <= 1836.1.1
    }

    immediate = {
        # creates a number of burgs equal to the number of urban centres in each state
        every_state = {
            limit = {
                has_building = building_urban_center
            }
            create_building = {
                building = building_burg
                level = {
                    
                }
            }
        }
    }
}